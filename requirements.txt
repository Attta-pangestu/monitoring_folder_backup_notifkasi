# Required packages for Database Backup Monitor
# Note: tkinter usually comes pre-installed with Python

# For email functionality
# No additional packages needed - uses built-in smtplib and email modules

# For database operations
# No additional packages needed - uses built-in sqlite3 module

# For GUI
# No additional packages needed - uses built-in tkinter module

# Optional: For enhanced email functionality
# secure-smtplib==0.2.5  # If needed for enhanced security

# Optional: For better email templates
# jinja2==3.1.2  # If needed for complex email templates